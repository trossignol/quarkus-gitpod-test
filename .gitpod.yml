
tasks:
- init: ./mvnw clean compile
  command: ./mvnw quarkus:dev

# exposed ports
ports:
- port: 8080
  onOpen: open-preview

vscode:
  extensions:
    - redhat.java
    - vscjava.vscode-java-debug
    - vscjava.vscode-java-test
